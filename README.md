# Mission Control E2E Test Target

Private, disposable target repository for isolated Mission Control and Cursor
Cloud Agent probes. It is not the Mission Control source repository.

The baseline intentionally has no third-party dependencies. `npm test` is the
required deterministic check.
